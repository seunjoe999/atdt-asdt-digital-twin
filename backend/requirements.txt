fastapi>=0.109
uvicorn[standard]>=0.27
python-multipart>=0.0.18
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
sqlalchemy>=2.0
psycopg[binary]>=3.1
PyJWT>=2.8
passlib[bcrypt]>=1.7
# passlib 1.7.4's bcrypt backend probes bcrypt.__about__, which bcrypt>=4.1
# removed; pin below that to avoid a version-detection warning/crash at
# import time until passlib ships a fix.
bcrypt<4.1
email-validator>=2.0
pypdf>=4.0
python-docx>=1.1
chromadb>=0.5
openai>=1.30
numpy>=1.26

# dyon supplies the multi-provider (OpenAI/Anthropic/Ollama/offline) LLM
# client used by the Teaching, Tutoring, and Examination generation layer.
dyon[agents]>=0.11
